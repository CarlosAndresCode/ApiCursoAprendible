models:
  Article:
    title: string
    slug: string unique
    content: string
    active: boolean
    category_id: id
    user_id: id
  Category:
    name: string
    slug: string unique
    active: boolean
    relationships:
      hasMany: Article
  # ...
